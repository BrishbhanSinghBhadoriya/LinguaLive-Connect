{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/lingua-live-next dev",
    "dev:api": "pnpm --filter @workspace/api-server dev",
    "dev:full": "concurrently \"pnpm dev:api\" \"pnpm dev\"",
    "build": "pnpm --filter @workspace/lingua-live-next build",
    "typecheck": "pnpm --filter @workspace/lingua-live-next typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@socket.io/component-emitter": "^3.1.2",
    "engine.io-client": "^6.6.5"
  }
}
